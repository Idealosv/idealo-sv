import { useEffect, useMemo, useState } from 'react'

const money = (value) => new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(value||0))
const dt = (value) => value ? new Date(value).toLocaleString('es-SV') : '—'
const newKey=()=>crypto.randomUUID()

export function DeliveriesModule({ company, supabase }) {
  const [orders,setOrders]=useState([]), [rows,setRows]=useState([]), [message,setMessage]=useState('')
  const [form,setForm]=useState({work_order_id:'',delivery_method:'PICKUP',scheduled_at:'',delivery_address:'',notes:''})
  const load=async()=>{
    const [o,d]=await Promise.all([
      supabase.from('work_orders').select('id,number,title,status,client_id,total,clients(name)').eq('company_id',company.id).in('status',['READY','DELIVERED']).order('created_at',{ascending:false}),
      supabase.from('deliveries').select('*,clients(name),work_orders(number,title,total,status)').eq('company_id',company.id).order('created_at',{ascending:false})
    ])
    if(o.error||d.error) setMessage(o.error?.message||d.error?.message); else {setOrders(o.data||[]);setRows(d.data||[])}
  }
  useEffect(()=>{load()},[company.id])
  const create=async(e)=>{e.preventDefault();const order=orders.find(x=>x.id===form.work_order_id);if(!order)return setMessage('Seleccioná una orden lista para entregar.');const {error}=await supabase.from('deliveries').insert({company_id:company.id,work_order_id:order.id,client_id:order.client_id,delivery_method:form.delivery_method,scheduled_at:form.scheduled_at||null,delivery_address:form.delivery_address||null,notes:form.notes||null,status:form.scheduled_at?'SCHEDULED':'READY'});if(error)setMessage(error.message);else{setMessage('Entrega creada.');setForm({work_order_id:'',delivery_method:'PICKUP',scheduled_at:'',delivery_address:'',notes:''});await load()}}
  const delivered=async(r)=>{const recipient=window.prompt('Nombre de quien recibe:','');if(recipient===null)return;const {error}=await supabase.from('deliveries').update({status:'DELIVERED',delivered_at:new Date().toISOString(),recipient_name:recipient||null,updated_at:new Date().toISOString()}).eq('id',r.id);if(!error&&r.work_order_id)await supabase.from('work_orders').update({status:'DELIVERED',updated_at:new Date().toISOString()}).eq('id',r.work_order_id);setMessage(error?error.message:'Entrega confirmada. La OT ya puede pasar directamente a Facturación.');await load()}
  const openBilling=r=>window.dispatchEvent(new CustomEvent('idealo-open-module',{detail:{target:'billing',tab:'emitir',workOrderId:r.work_order_id||'',workOrderNumber:r.work_orders?.number||'',clientId:r.client_id||'',clientName:r.clients?.name||''}}))
  const open=rows.filter(r=>r.status!=='DELIVERED'&&r.status!=='CANCELLED').length
  return <section className="clients-module"><div className="clients-titlebar"><div><p className="form-kicker">LOGÍSTICA</p><h2>Entregas</h2><p>Controla retiro, envío o instalación del trabajo terminado y registra quién lo recibió.</p></div><span className="status dte-ready">{open} pendientes</span></div>{message&&<p className="feedback success">{message}</p>}<form className="panel client-form-full" onSubmit={create}><div className="form-grid three"><label className="field form-span-2"><span>Orden lista *</span><select required value={form.work_order_id} onChange={e=>setForm({...form,work_order_id:e.target.value)}><option value="">Seleccionar orden</option>{orders.filter(o=>!rows.some(r=>r.work_order_id===o.id)).map(o=><option key={o.id} value={o.id}>OT-{String(o.number).padStart(5,'0')} · {o.clients?.name||'Cliente'} · {o.title}</option>)}</select></label><label className="field"><span>Modalidad</span><select value={form.delivery_method} onChange={e=>setForm({...form,delivery_method:e.target.value)}><option value="PICKUP">Retiro en local</option><option value="DELIVERY">Envío</option><option value="INSTALLATION">Instalación</option></select></label><label className="field"><span>Fecha programada</span><input type="datetime-local" value={form.scheduled_at} onChange={e=>setForm({...form,scheduled_at:e.target.value)}/></label><label className="field form-span-2"><span>Dirección / lugar</span><input value={form.delivery_address} onChange={e=>setForm({...form,delivery_address:e.target.value)}/></label><label className="field form-span-3"><span>Indicaciones</span><textarea rows="2" value={form.notes} onChange={e=>setForm({...form,notes:e.target.value)}/></label></div><div className="form-actions end"><button>Crear entrega</button></div></form><section className="panel"><div className="panel-heading"><div><p className="form-kicker">SEGUIMIENTO</p><h3>Entregas recientes</h3></div></div>{rows.length?<div className="client-list">{rows.map(r=><div className="client-row" key={r.id}><div><strong>ENT-{String(r.number).padStart(5,'0')} · {r.clients?.name||'Cliente'}</strong><small>{r.work_orders?.title||'Trabajo'} · {r.delivery_method} · programada {dt(r.scheduled_at)}</small><small>{r.status==='DELIVERED'?`Recibió: ${r.recipient_name||'Sin nombre'} · ${dt(r.delivered_at)}`:r.delivery_address||'Sin dirección adicional'}</small></div><div><strong>{r.status}</strong>{r.status!=='DELIVERED'&&r.status!=='CANCELLED'&&<button type="button" onClick={()=>delivered(r)}>Confirmar entrega</button>}{r.status==='DELIVERED'&&r.work_order_id&&<button type="button" onClick={()=>openBilling(r)}>Facturar este trabajo</button>}</div></div>)}</div>:<div className="empty-state"><strong>Sin entregas</strong><p>Cuando una orden llegue a LISTO podrás programar su entrega.</p></div>}</section></section>
}

export function ReceivablesModule({ company, supabase }) {
  const [orders,setOrders]=useState([]),[rows,setRows]=useState([]),[payments,setPayments]=useState([]),[accounts,setAccounts]=useState([]),[message,setMessage]=useState(''),[busy,setBusy]=useState(false)
  const [form,setForm]=useState({work_order_id:'',concept:'',amount_total:'',due_date:''})
  const [pay,setPay]=useState({receivable_id:'',cash_account_id:'',amount:'',payment_method:'CASH',reference:'',notes:'',payment_key:newKey()})
  const load=async()=>{
    const [o,r,p,a]=await Promise.all([
      supabase.from('work_orders').select('id,number,title,total,client_id,clients(name)').eq('company_id',company.id).order('created_at',{ascending:false}),
      supabase.from('accounts_receivable').select('*,clients(name),work_orders(number,title),dte_documents(control_number,dte_type,status)').eq('company_id',company.id).order('created_at',{ascending:false}),
      supabase.from('customer_payments').select('*,cash_accounts(name)').eq('company_id',company.id).order('paid_at',{ascending:false}).limit(100),
      supabase.from('cash_accounts').select('id,name,account_type,active').eq('company_id',company.id).eq('active',true).order('name')
    ])
    if(o.error||r.error||p.error||a.error)setMessage(o.error?.message||r.error?.message||p.error?.message||a.error?.message);else{setOrders(o.data||[]);setRows(r.data||[]);setPayments(p.data||[]);setAccounts(a.data||[]);setPay(v=>({...v,cash_account_id:v.cash_account_id||a.data?.[0]?.id||''}))}
  }
  useEffect(()=>{load()},[company.id])
  const selectOrder=(id)=>{const o=orders.find(x=>x.id===id);setForm({work_order_id:id,concept:o?`OT-${String(o.number).padStart(5,'0')} · ${o.title}`:'',amount_total:o?.total||'',due_date:''})}
  const create=async(e)=>{e.preventDefault();const o=orders.find(x=>x.id===form.work_order_id);if(!o)return setMessage('Seleccioná una orden.');const {error}=await supabase.from('accounts_receivable').insert({company_id:company.id,client_id:o.client_id,work_order_id:o.id,concept:form.concept,amount_total:Number(form.amount_total||0),due_date:form.due_date||null});if(error)setMessage(error.message);else{setMessage('Cuenta por cobrar creada.');setForm({work_order_id:'',concept:'',amount_total:'',due_date:''});await load()}}
  const chooseReceivable=id=>{const r=rows.find(x=>x.id===id);const balance=r?Math.max(0,Number(r.amount_total)-Number(r.amount_paid)):0;setPay(v=>({...v,receivable_id:id,amount:balance?balance.toFixed(2):''}))}
  const registerPayment=async(e)=>{e.preventDefault();if(busy)return;const r=rows.find(x=>x.id===pay.receivable_id);if(!r)return setMessage('Seleccioná una cuenta.');if(!pay.cash_account_id)return setMessage('Seleccioná la caja o banco donde ingresará el cobro.');const balance=Math.max(0,Number(r.amount_total)-Number(r.amount_paid));const amount=Number(pay.amount||0);if(amount<=0||amount>balance+0.001)return setMessage(`El pago debe ser mayor a $0 y no superar ${money(balance)}.`);setBusy(true);setMessage('');const {error}=await supabase.rpc('register_customer_payment',{p_receivable:r.id,p_cash_account:pay.cash_account_id,p_amount:amount,p_payment_method:pay.payment_method,p_reference:pay.reference||null,p_notes:pay.notes||null,p_payment_key:pay.payment_key});if(error)setMessage(error.message);else{setMessage('Cobro aplicado y entrada registrada en Caja.');setPay(v=>({...v,receivable_id:'',amount:'',reference:'',notes:'',payment_key:newKey()}));await load()}setBusy(false)}
  const pending=useMemo(()=>rows.filter(r=>!['PAID','CANCELLED'].includes(r.status)).reduce((s,r)=>s+Math.max(0,Number(r.amount_total)-Number(r.amount_paid)),0),[rows])
  const collected=useMemo(()=>payments.reduce((s,p)=>s+Number(p.amount||0),0),[payments])
  return <section className="clients-module"><div className="clients-titlebar"><div><p className="form-kicker">FINANZAS</p><h2>Cuentas por cobrar</h2><p>Las facturas DTE a crédito aceptadas generan cartera automáticamente; los cobros entran a Caja con trazabilidad.</p></div></div><div className="metrics-grid"><article className="metric-card"><span>Saldo pendiente</span><strong>{money(pending)}</strong></article><article className="metric-card"><span>Cobrado registrado</span><strong>{money(collected)}</strong></article><article className="metric-card"><span>Cuentas abiertas</span><strong>{rows.filter(r=>!['PAID','CANCELLED'].includes(r.status)).length}</strong></article></div>{message&&<p className={/aplicado|creada/i.test(message)?'feedback success':'feedback error'}>{message}</p>}<div className="module-grid two-column"><form className="panel" onSubmit={create}><div className="panel-heading"><div><p className="form-kicker">CUENTA MANUAL</p><h3>Generar saldo desde OT</h3></div></div><div className="form-grid"><label className="field"><span>Orden de trabajo *</span><select required value={form.work_order_id} onChange={e=>selectOrder(e.target.value)}><option value="">Seleccionar orden</option>{orders.filter(o=>!rows.some(r=>r.work_order_id===o.id)).map(o=><option key={o.id} value={o.id}>OT-{String(o.number).padStart(5,'0')} · {o.clients?.name||'Cliente'} · {money(o.total)}</option>)}</select></label><label className="field"><span>Concepto *</span><input required value={form.concept} onChange={e=>setForm({...form,concept:e.target.value)}/></label><label className="field"><span>Total *</span><input type="number" min="0" step="0.01" required value={form.amount_total} onChange={e=>setForm({...form,amount_total:e.target.value)}/></label><label className="field"><span>Vencimiento</span><input type="date" value={form.due_date} onChange={e=>setForm({...form,due_date:e.target.value)}/></label></div><div className="form-actions end"><button>Crear cuenta</button></div></form><form className="panel" onSubmit={registerPayment}><div className="panel-heading"><div><p className="form-kicker">COBRO</p><h3>Registrar abono o pago</h3></div></div><div className="form-grid"><label className="field"><span>Cuenta *</span><select required value={pay.receivable_id} onChange={e=>chooseReceivable(e.target.value)}><option value="">Seleccionar cuenta</option>{rows.filter(r=>!['PAID','CANCELLED'].includes(r.status)).map(r=><option key={r.id} value={r.id}>CXC-{String(r.number).padStart(5,'0')} · {r.clients?.name||'Cliente'} · saldo {money(Number(r.amount_total)-Number(r.amount_paid))}</option>)}</select></label><label className="field"><span>Caja / banco *</span><select required value={pay.cash_account_id} onChange={e=>setPay({...pay,cash_account_id:e.target.value)}><option value="">Seleccionar</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name} · {a.account_type}</option>)}</select></label><label className="field"><span>Monto *</span><input type="number" min="0.01" step="0.01" required value={pay.amount} onChange={e=>setPay({...pay,amount:e.target.value)}/></label><label className="field"><span>Forma</span><select value={pay.payment_method} onChange={e=>setPay({...pay,payment_method:e.target.value)}><option value="CASH">Efectivo</option><option value="TRANSFER">Transferencia</option><option value="CARD">Tarjeta</option><option value="CHECK">Cheque</option><option value="OTHER">Otro</option></select></label><label className="field"><span>Referencia</span><input value={pay.reference} onChange={e=>setPay({...pay,reference:e.target.value)}/></label><label className="field"><span>Notas</span><input value={pay.notes} onChange={e=>setPay({...pay,notes:e.target.value)}/></label></div><div className="form-actions end"><button disabled={busy||!accounts.length}>{busy?'Registrando…':'Registrar cobro y entrada de caja'}</button></div>{!accounts.length&&<div className="dte-note">Primero crea una cuenta activa en Caja para registrar el ingreso.</div>}</form></div><section className="panel"><div className="panel-heading"><div><p className="form-kicker">CARTERA</p><h3>Saldos de clientes</h3></div></div>{rows.length?<div className="client-list">{rows.map(r=>{const bal=Math.max(0,Number(r.amount_total)-Number(r.amount_paid));return <div className="client-row" key={r.id}><div><strong>CXC-{String(r.number).padStart(5,'0')} · {r.clients?.name||'Cliente'}</strong><small>{r.dte_documents?`${r.dte_documents.control_number} · `:''}{r.concept} · vence {r.due_date||'sin fecha'}</small></div><div><strong>{money(bal)} pendiente</strong><small>{money(r.amount_paid)} pagado de {money(r.amount_total)} · {r.status}</small>{bal>0&&<button type="button" onClick={()=>chooseReceivable(r.id)}>Cobrar</button>}</div></div>})}</div>:<div className="empty-state"><strong>Sin cuentas por cobrar</strong></div>}</section><section className="panel"><div className="panel-heading"><div><p className="form-kicker">COBROS</p><h3>Últimos ingresos de clientes</h3></div></div>{payments.length?<div className="client-list">{payments.slice(0,30).map(p=><div className="client-row" key={p.id}><div><strong>{money(p.amount)} · {p.payment_method}</strong><small>{dt(p.paid_at)} · {p.cash_accounts?.name||'Caja'} · {p.reference||'Sin referencia'}</small></div></div>)}</div>:<div className="empty-state"><strong>Sin cobros registrados</strong></div>}</section></section>
}
