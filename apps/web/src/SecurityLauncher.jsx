import { useEffect, useMemo, useState } from 'react'
import { supabase } from './lib/supabase.js'
export default function SecurityLauncher(){
 const [open,setOpen]=useState(false),[session,setSession]=useState(null),[company,setCompany]=useState(null),[members,setMembers]=useState([]),[msg,setMsg]=useState('')
 useEffect(()=>{if(!supabase)return;supabase.auth.getSession().then(({data})=>setSession(data.session||null));const {data:l}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>l.subscription.unsubscribe()},[])
 useEffect(()=>{if(!session||!supabase){setCompany(null);setMembers([]);return}let live=true;(async()=>{setMsg('');const {data,error}=await supabase.rpc('get_my_companies');if(!live)return;if(error){setMsg(error.message);return}const current=data?.[0]||null;setCompany(current);if(!current){setMembers([]);return}const {data:m,error:memberError}=await supabase.from('company_members').select('company_id,user_id,role,created_at').eq('company_id',current.id).order('created_at',{ascending:true});if(!live)return;if(memberError)setMsg(memberError.message);else setMembers(m||[])})();return()=>{live=false}},[session])
 useEffect(()=>{const fn=e=>{if((e.detail||{}).target==='security')setOpen(true)};window.addEventListener('idealo-open-module',fn);return()=>window.removeEventListener('idealo-open-module',fn)},[])
 const risk=useMemo(()=>({admins:members.filter(x=>['OWNER','ADMIN'].includes(String(x.role).toUpperCase())).length,staff:members.filter(x=>String(x.role).toUpperCase()==='STAFF').length,viewers:members.filter(x=>String(x.role).toUpperCase()==='VIEWER').length}),[members])
 if(!open)return null
 return <div className="erp-modal-backdrop" role="presentation" onMouseDown={()=>setOpen(false)}><section className="erp-modal-panel" role="dialog" aria-modal="true" aria-label="Seguridad" onMouseDown={e=>e.stopPropagation()}><header className="erp-modal-head"><div><strong>Seguridad</strong><small>Acceso, usuarios y controles críticos del ERP</small></div><button type="button" className="erp-modal-close" onClick={()=>setOpen(false)}>×</button></header><div className="erp-modal-body"><section className="panel"><div className="clients-titlebar"><div><p className="form-kicker">CENTRO DE SEGURIDAD</p><h2>Estado de protección</h2><p>Identidad, aislamiento empresarial, privilegios y credenciales sensibles.</p></div><span className={session&&company?'status dte-ready':'status dte-pending'}>{session&&company?'Acceso validado':'Revisar acceso'}</span></div>{msg&&<p className="feedback error">{msg}</p>}<div className="metric-grid"><article className="metric-card"><small>Sesión</small><strong>{session?'Activa':'No disponible'}</strong><span>Supabase Auth</span></article><article className="metric-card"><small>Empresa</small><strong>{company?'Aislada':'Pendiente'}</strong><span>Contexto company_id</span></article><article className="metric-card"><small>Miembros</small><strong>{members.length}</strong><span>{risk.admins} con privilegio alto</span></article><article className="metric-card"><small>Acceso limitado</small><strong>{risk.viewers}</strong><span>{risk.staff} personal operativo</span></article></div></section><div className="module-grid two-column"><section className="panel"><p className="form-kicker">CONTROLES CRÍTICOS</p><h3>Protección del ERP</h3><div className="schedule-list"><article className="schedule-card"><div><strong>Datos por empresa</strong><small>Consultas operativas aisladas por company_id y políticas RLS.</small></div><span className="status dte-ready">Activo</span></article><article className="schedule-card"><div><strong>Credenciales DTE</strong><small>Certificado, claves y secretos permanecen únicamente en servidor.</small></div><span className="status dte-ready">Servidor</span></article><article className="schedule-card"><div><strong>Sesión autenticada</strong><small>{session?.user?.email||'Sin identidad disponible'}</small></div><span className={session?'status dte-ready':'status dte-pending'}>{session?'OK':'Revisar'}</span></article></div></section><section className="panel"><p className="form-kicker">USUARIOS Y PRIVILEGIOS</p><h3>Resumen de accesos</h3>{members.slice(0,12).map(m=><div className="workload-row" key={`${m.company_id}-${m.user_id}`}><div><strong>{String(m.role||'viewer').toUpperCase()}</strong><small>{String(m.user_id).slice(0,8)}… · desde {String(m.created_at||'').slice(0,10)||'—'}</small></div><span className={['owner','admin'].includes(String(m.role).toLowerCase())?'status dte-pending':'status dte-ready'}>{['owner','admin'].includes(String(m.role).toLowerCase())?'Privilegio alto':'Estándar'}</span></div>)}{!members.length&&<div className="empty-state"><strong>Sin membresías visibles</strong></div>}</section></div></div></section></div>
}